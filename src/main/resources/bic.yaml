server:
  applicationContextPath: /api
  applicationConnectors:
    - type: https
      port: 8443
      keyStorePath: .keystore
      keyStorePassword: wizardfingers
      keyStoreType: JKS

backupConfiguration:
  backupsToKeep: 3
  backupLocation: db_backups
  backupFrequency:
    frequency: WEEKLY
    day: 1

emailConfiguration:
  smtpServer: smtp.example.com
  senderName: bic_admin
  senderAddress: bic_main@hs.edu
  smtpPort: 465

dbConfiguration:
  port: 27017
  host: localhost
